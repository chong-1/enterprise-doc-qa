# 企业文档智能问答平台 — 实现步骤

## 一、项目概述

面向企业的私有化文档智能问答系统，支持多格式文档上传、RAG 检索增强生成、Agent 智能编排、多轮对话记忆、RBAC 权限管理。适合本地开发部署，技术栈精简实用，简历亮点密集。

---

## 二、调整后的技术栈

| 层次 | 技术 | 说明 |
|------|------|------|
| **后端框架** | Python 3.12 + FastAPI | 异步高性能，自动生成 OpenAPI 文档 |
| **关系数据库** | MySQL 8.0 | 存储用户、文档元数据、知识库、对话记录 |
| **ORM** | SQLAlchemy 2.0 (async) + aiomysql | 异步驱动，类型安全 |
| **数据库迁移** | Alembic | 版本化管理表结构 |
| **向量数据库** | Chroma | 轻量、嵌入式运行、无需单独部署、适合本地开发 |
| **Embedding 模型** | BGE-M3 (bge-m3) | 中英双语、支持稠密+稀疏向量、1024→8192 维自适应、8K 上下文 |
| **Reranker** | BGE-Reranker-v2-m3 | 检索重排序，提升召回精度 |
| **LLM 后端** | 双模式：OpenAI API 兼容接口 / Ollama 本地模型 | 开发用 Ollama(qwen2.5)，生产可切 API |
| **异步任务队列** | Celery + Redis Broker | 文档解析、Embedding 生成异步解耦 |
| **缓存 & 会话** | Redis 7.x | 对话上下文缓存、限流计数器、Session 存储 |
| **对象存储** | MinIO (S3 兼容) | 私有化文档文件存储，本地开发可选本地文件系统 |
| **Agent 框架** | LangGraph + 自研轻量编排 | 复杂查询多步推理、工具调用 |
| **OCR** | PaddleOCR | 扫描件/图片文字提取 |
| **容器化** | Docker Compose | 一键启动全部服务 |
| **前端（可选）** | Vue 3 + Element Plus | 管理后台 + 问答界面 |

### 为什么选 Chroma？

- **零部署成本**：`pip install chromadb` 即可，嵌入式运行
- **本地开发友好**：数据持久化到本地磁盘，无需额外服务进程
- **API 简洁**：与 LangChain/LlamaIndex 深度集成
- **够用**：十万级文档以内性能完全满足；生产可平滑迁移到 Milvus

### 为什么选 BGE-M3？

- **稠密+稀疏混合**：单一模型同时输出 Dense（1024d）和 Sparse（词汇权重）向量
- **多语言**：中英双语训练，中文语义理解 SOTA
- **长上下文**：支持 8192 token 输入，适合长文档段落
- **生态成熟**：FlagEmbedding 官方支持，与 LangChain 无缝对接

---

## 三、系统架构图

```
┌─────────────────────────────────────────────────────────┐
│                    前端 (Vue 3 + Element Plus)              │
├─────────────────────────────────────────────────────────┤
│                  Nginx (反向代理 + 静态资源)                 │
├─────────────────────────────────────────────────────────┤
│                    FastAPI 后端 (核心)                      │
│                                                         │
│  ┌──────────┬──────────┬──────────┬──────────────┐      │
│  │ 用户认证  │ 文档管理  │ RAG 问答  │ Agent 编排   │      │
│  │ JWT+RBAC │ CRUD API │ 混合检索  │ LangGraph    │      │
│  └──────────┴──────────┴──────────┴──────────────┘      │
│                                                         │
│  ┌──────────┬──────────┬──────────┬──────────────┐      │
│  │ 知识库   │ 对话管理  │ 审计日志  │ 数据看板      │      │
│  │ 多库隔离  │ 上下文记忆 │ 操作追溯  │ 使用统计      │      │
│  └──────────┴──────────┴──────────┴──────────────┘      │
├─────────────────────────────────────────────────────────┤
│                       中间件层                            │
│  ┌──────────┬──────────┬──────────┐                    │
│  │  Redis   │  Celery  │  MinIO   │                    │
│  │ 缓存/会话 │ 异步任务  │ 文件存储  │                    │
│  └──────────┴──────────┴──────────┘                    │
├─────────────────────────────────────────────────────────┤
│                       数据层                              │
│  ┌──────────┬──────────┬──────────┐                    │
│  │  MySQL   │  Chroma  │  本地FS  │                    │
│  │ 业务数据  │ 向量存储  │ 文件存储  │                    │
│  └──────────┴──────────┴──────────┘                    │
├─────────────────────────────────────────────────────────┤
│                       AI 层                               │
│  ┌──────────┬──────────┬──────────┐                    │
│  │ BGE-M3   │  LLM     │Reranker  │                    │
│  │Embedding │ Ollama/  │BGE-Rerank│                    │
│  │          │ API      │  er-v2   │                    │
│  └──────────┴──────────┴──────────┘                    │
└─────────────────────────────────────────────────────────┘
```

---

## 四、项目目录结构

```
enterprise-qa-platform/
├── backend/
│   ├── app/
│   │   ├── __init__.py
│   │   ├── main.py                    # FastAPI 入口
│   │   ├── api/
│   │   │   ├── __init__.py
│   │   │   ├── v1/
│   │   │   │   ├── __init__.py
│   │   │   │   ├── auth.py            # 登录/注册/Token 刷新
│   │   │   │   ├── users.py           # 用户管理
│   │   │   │   ├── knowledge_bases.py # 知识库 CRUD
│   │   │   │   ├── documents.py       # 文档上传/解析/管理
│   │   │   │   ├── qa.py              # 问答接口(SSE 流式)
│   │   │   │   ├── conversations.py   # 对话历史管理
│   │   │   │   └── admin.py           # 管理后台(统计/审计)
│   │   ├── core/
│   │   │   ├── __init__.py
│   │   │   ├── config.py             # pydantic-settings 配置
│   │   │   ├── security.py           # JWT 生成/校验、密码哈希
│   │   │   ├── dependencies.py       # 依赖注入(get_db/get_current_user)
│   │   │   └── exceptions.py         # 全局异常处理
│   │   ├── models/
│   │   │   ├── __init__.py
│   │   │   ├── base.py               # SQLAlchemy Base + Mixin
│   │   │   ├── user.py               # 用户表
│   │   │   ├── role.py               # 角色表
│   │   │   ├── knowledge_base.py     # 知识库表
│   │   │   ├── document.py           # 文档表
│   │   │   ├── conversation.py       # 对话表
│   │   │   ├── message.py            # 消息表
│   │   │   └── audit_log.py          # 审计日志表
│   │   ├── schemas/
│   │   │   ├── __init__.py
│   │   │   ├── auth.py               # 登录请求/响应
│   │   │   ├── user.py
│   │   │   ├── knowledge_base.py
│   │   │   ├── document.py
│   │   │   ├── qa.py                 # 问答请求/流式响应
│   │   │   └── common.py             # 分页/通用响应
│   │   ├── services/
│   │   │   ├── __init__.py
│   │   │   ├── auth_service.py       # 认证逻辑
│   │   │   ├── document/
│   │   │   │   ├── __init__.py
│   │   │   │   ├── parser.py         # 多格式解析(PDF/Word/Excel/MD/TXT)
│   │   │   │   ├── chunker.py        # 智能分块(语义分块)
│   │   │   │   └── loader.py         # 文件上传/存储
│   │   │   ├── rag/
│   │   │   │   ├── __init__.py
│   │   │   │   ├── embedding.py      # BGE-M3 Embedding 封装
│   │   │   │   ├── retriever.py      # 混合检索(稠密+稀疏)
│   │   │   │   ├── reranker.py       # BGE-Reranker 重排序
│   │   │   │   ├── generator.py      # LLM 生成封装
│   │   │   │   └── pipeline.py       # RAG 完整流水线编排
│   │   │   ├── agent/
│   │   │   │   ├── __init__.py
│   │   │   │   ├── router.py         # 意图路由 Agent
│   │   │   │   ├── tools.py          # Agent 工具集(检索/计算/查库)
│   │   │   │   ├── graph.py          # LangGraph 状态图编排
│   │   │   │   └── memory.py         # 对话记忆管理
│   │   │   └── llm/
│   │   │       ├── __init__.py
│   │   │       ├── base.py           # LLM 抽象基类
│   │   │       ├── openai_backend.py # OpenAI 兼容后端
│   │   │       └── ollama_backend.py # Ollama 本地后端
│   │   ├── db/
│   │   │   ├── __init__.py
│   │   │   ├── session.py            # MySQL 异步会话
│   │   │   ├── chroma_store.py       # Chroma 向量存储操作
│   │   │   └── redis_client.py       # Redis 客户端封装
│   │   └── middleware/
│   │       ├── __init__.py
│   │       ├── cors.py               # CORS 配置
│   │       ├── logging.py            # 请求日志中间件
│   │       └── rate_limit.py         # 限流中间件
│   ├── tasks/
│   │   ├── __init__.py
│   │   ├── celery_app.py             # Celery 配置
│   │   ├── document_tasks.py         # 文档处理异步任务
│   │   └── embedding_tasks.py        # Embedding 生成任务
│   ├── tests/
│   │   ├── __init__.py
│   │   ├── conftest.py               # pytest fixtures
│   │   ├── test_auth.py
│   │   ├── test_documents.py
│   │   ├── test_rag.py
│   │   └── test_agent.py
│   ├── alembic/
│   │   ├── env.py
│   │   └── versions/
│   ├── alembic.ini
│   ├── Dockerfile
│   ├── requirements.txt
│   └── pyproject.toml
├── frontend/                          # 前端（Vue 3）
│   ├── src/
│   │   ├── views/                     # 页面
│   │   ├── components/                # 组件
│   │   ├── api/                       # API 封装
│   │   ├── router/                    # 路由
│   │   └── stores/                    # Pinia 状态管理
│   ├── package.json
│   └── vite.config.ts
├── docker/
│   ├── docker-compose.yml             # 开发环境
│   ├── docker-compose.prod.yml        # 生产环境
│   ├── mysql/
│   │   └── init.sql                   # 初始化 SQL
│   ├── nginx/
│   │   ├── nginx.conf
│   │   └── default.conf
│   └── redis/
│       └── redis.conf
├── scripts/
│   ├── init_db.py                     # 数据库初始化
│   ├── seed_data.py                   # 测试数据填充
│   └── download_models.py             # 模型下载脚本
├── .env.example                       # 环境变量模板
├── .gitignore
└── README.md
```

---

## 五、数据库表设计

### 5.1 ER 概要

```
users ──┬── user_roles ──── roles ──── role_permissions ──── permissions
        │
        ├── knowledge_bases (owner)
        │       │
        │       └── documents (1:N)
        │
        └── conversations
                │
                └── messages (1:N)
                        │
                        └── message_citations (引用来源)
audit_logs (操作审计)
```

### 5.2 核心表结构

#### users
| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK | 主键 |
| username | VARCHAR(50) UNIQUE | 用户名 |
| email | VARCHAR(255) UNIQUE | 邮箱 |
| hashed_password | VARCHAR(255) | 密码哈希 |
| is_active | BOOLEAN | 是否激活 |
| is_superuser | BOOLEAN | 超管标记 |
| created_at / updated_at | DATETIME | 时间戳 |

#### knowledge_bases
| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK | 主键 |
| name | VARCHAR(200) | 知识库名称 |
| description | TEXT | 描述 |
| owner_id | FK→users.id | 创建者 |
| embedding_model | VARCHAR(100) | 使用的 Embedding 模型 |
| chunk_size / chunk_overlap | INT | 分块配置 |
| is_public | BOOLEAN | 是否公开 |
| created_at / updated_at | DATETIME | 时间戳 |

#### documents
| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK | 主键 |
| kb_id | FK→knowledge_bases.id | 所属知识库 |
| filename | VARCHAR(500) | 原始文件名 |
| file_type | VARCHAR(20) | pdf/docx/xlsx/md/txt |
| file_size | BIGINT | 文件大小(bytes) |
| storage_path | VARCHAR(1000) | MinIO/本地存储路径 |
| chunk_count | INT | 分块数量 |
| status | ENUM | pending/processing/completed/failed |
| created_at / updated_at | DATETIME | 时间戳 |

#### conversations
| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK | 主键 |
| user_id | FK→users.id | 用户 |
| kb_id | FK→knowledge_bases.id | 关联知识库 |
| title | VARCHAR(500) | 对话标题 |
| created_at / updated_at | DATETIME | 时间戳 |

#### messages
| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK | 主键 |
| conversation_id | FK→conversations.id | 所属对话 |
| role | ENUM | user/assistant/system |
| content | LONGTEXT | 消息内容 |
| tokens_used | INT | Token 消耗 |
| processing_time_ms | INT | 处理耗时 |
| created_at | DATETIME | 时间戳 |

#### message_citations
| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK | 主键 |
| message_id | FK→messages.id | 所属消息 |
| document_id | FK→documents.id | 引用文档 |
| chunk_index | INT | 引用分块索引 |
| score | FLOAT | 检索相关度分数 |
| cited_text | TEXT | 引用原文片段 |

---

## 六、核心流程设计

### 6.1 文档入库流程

```
1. 用户上传文件 → MinIO/本地临时存储
2. 写入 documents 表 (status=pending)
3. Celery 异步任务：
   ├── 格式检测 → 选择解析器
   ├── 提取文本 (PDF→PyMuPDF, Word→python-docx, Excel→openpyxl)
   ├── 文本清洗 (去噪/标准化/保留表格结构)
   ├── 语义分块 (BGE-M3 tokenizer 感知分块, 默认 chunk_size=512, overlap=64)
   ├── 调用 BGE-M3 生成 Embedding (dense + sparse)
   ├── 写入 Chroma (每个 chunk 一条记录, 携带 metadata)
   └── 更新 documents.status = completed
4. 用户可查询处理状态
```

### 6.2 RAG 问答流程

```
1. 用户提问 → 可选 Query Rewrite (HyDE 或 LLM 改写)
2. 混合检索：
   ├── Dense 检索：BGE-M3 稠密向量, Chroma 相似度搜索 (top_k=20)
   └── Sparse 检索：BGE-M3 稀疏向量, BM25 关键词匹配 (top_k=20)
3. 融合排序：RRF (Reciprocal Rank Fusion) 合并两路结果
4. Reranker 重排：BGE-Reranker-v2-m3 精排 (top_k=5)
5. 上下文组装：模板拼接 + token 预算控制
6. LLM 生成：流式输出 (SSE), 带引用标注
7. 保存消息 + 引用到数据库
```

### 6.3 Agent 路由流程

```
用户提问
    │
    ▼
Router Agent (意图识别)
    │
    ├── 简单问答 ──→ RAG Pipeline (直接检索→生成)
    │
    ├── 对比分析 ──→ Multi-doc Agent (多文档检索→并行阅读→综合回答)
    │
    ├── 结构化查询 ──→ SQL Agent (Text-to-SQL, 查询 MySQL 业务数据)
    │
    ├── 总结摘要 ──→ Summary Agent (Map-Reduce / Refine 链)
    │
    └── 复杂推理 ──→ ReAct Agent (思考→行动→观察 循环, 最多 5 步)
```

---

## 七、分阶段实施计划

### Phase 1：基础设施搭建（第 1 周）

**目标**：Docker 环境跑通，FastAPI 启动，数据库就绪

#### 任务清单
- [ ] 初始化 Python 项目结构 (pyproject.toml / requirements.txt)
- [ ] 编写 Docker Compose (MySQL + Redis + MinIO + backend)
- [ ] FastAPI 入口文件 + 健康检查接口
- [ ] 配置管理 (`pydantic-settings`, `.env` 加载)
- [ ] MySQL 数据库模型定义 (User / Role / Permission)
- [ ] Alembic 初始迁移
- [ ] SQLAlchemy 异步引擎 + 会话工厂
- [ ] Redis 客户端封装
- [ ] 全局异常处理 + 统一响应格式
- [ ] CORS + 请求日志中间件

**验证标准**：
```bash
docker compose up -d          # 全部服务正常启动
curl http://localhost:8000/health  # {"status":"ok"}
curl http://localhost:8000/api/v1/docs  # Swagger 文档页
```

---

### Phase 2：用户认证与权限（第 1 周）

**目标**：JWT 认证 + RBAC 权限框架

#### 任务清单
- [ ] 密码哈希 (passlib + bcrypt)
- [ ] JWT 生成/校验 (python-jose)
- [ ] 注册/登录/Token 刷新 API
- [ ] 获取当前用户依赖注入 (`get_current_user`)
- [ ] RBAC 模型：User ↔ Role ↔ Permission 多对多
- [ ] 权限装饰器 (`@require_permission("document:upload")`)
- [ ] 初始化默认角色（admin / editor / viewer）

**验证标准**：
```bash
# 注册 → 登录 → 获取 Token → 访问受保护接口
curl -X POST /api/v1/auth/register -d '{"username":"test","password":"...","email":"test@test.com"}'
curl -X POST /api/v1/auth/login -d '{"username":"test","password":"..."}'  # 返回 JWT
curl /api/v1/users/me -H "Authorization: Bearer <token>"  # 返回用户信息
```

---

### Phase 3：文档处理管道（第 2 周）

**目标**：上传 → 解析 → 分块 → 全流程跑通

#### 任务清单
- [ ] MinIO 客户端封装（或本地文件存储）
- [ ] 文档上传 API + 文件类型校验
- [ ] 多格式解析器：
  - PDF: PyMuPDF (fitz)
  - Word: python-docx
  - Excel: openpyxl
  - Markdown: markdown-it-py
  - TXT: 直接读取
- [ ] 文本清洗工具（去空行/乱码/特殊字符）
- [ ] BGE-M3 感知的语义分块器：
  - 基于句子边界分割
  - chunk_size=512 tokens, overlap=64 tokens
  - 保留上下文（前后各一段）
- [ ] Celery 配置 + 文档处理任务
- [ ] 文档状态查询 API

**验证标准**：
```bash
# 上传 PDF → 返回 document_id → 查询状态 → completed
curl -X POST /api/v1/kb/1/documents -F "file=@test.pdf"
curl /api/v1/documents/{id}/status  # {"status":"completed","chunk_count":42}
```

---

### Phase 4：RAG 核心引擎（第 3-4 周）

**目标**：端到端问答跑通，答案准确且带引用

这是整个项目最核心的部分，需要细致打磨。

#### 任务清单

**4.1 Embedding 服务**
- [ ] BGE-M3 模型加载（sentence-transformers / FlagEmbedding）
- [ ] Dense Embedding 生成（1024 维）
- [ ] Sparse Embedding 生成（词汇权重字典）
- [ ] 批量处理 + GPU 加速支持
- [ ] 模型热加载/缓存

**4.2 Chroma 向量存储**
- [ ] Chroma 客户端初始化（PersistentClient）
- [ ] Collection 管理（按知识库创建/删除 Collection）
- [ ] 文档 chunk 写入（含 metadata: doc_id, chunk_idx, filename, page 等）
- [ ] 相似度检索（支持过滤条件：knowledge_base_id）
- [ ] Collection 全量删除/统计

**4.3 检索管道**
- [ ] Dense 检索：Chroma 语义相似度 (top_k=20)
- [ ] Sparse 检索：BM25 关键词匹配 (top_k=20)
- [ ] RRF (Reciprocal Rank Fusion) 融合算法
- [ ] BGE-Reranker-v2-m3 重排序 (top_k=5)
- [ ] 结果去重 + 最小分数阈值过滤

**4.4 生成管道**
- [ ] LLM Backend 抽象层（支持 OpenAI / Ollama 切换）
- [ ] System Prompt 模板设计（含角色设定 + 引用格式要求）
- [ ] 上下文窗口 token 预算计算（总窗口 − 预留生成空间）
- [ ] SSE 流式输出
- [ ] 引用提取 + 后处理格式化

**4.5 问答 API**
- [ ] 单次问答接口
- [ ] 流式问答接口 (SSE)
- [ ] 对话历史传递（最近 N 轮）
- [ ] 引用溯源返回（来源文档 + 原文片段 + 页码）

**验证标准**：
```bash
# 上传文档后提问
curl -X POST /api/v1/kb/1/qa -d '{
  "question": "公司2024年的营收目标是多少？",
  "stream": false
}'
# 返回:
{
  "answer": "根据《2024年度经营计划.pdf》，公司2024年营收目标为5.2亿元...",
  "citations": [
    {"document":"2024年度经营计划.pdf","page":3,"text":"...","score":0.92}
  ],
  "processing_time_ms": 1234
}
```

---

### Phase 5：Agent 智能编排（第 5 周）

**目标**：复杂查询的自动分解与多步推理

#### 任务清单
- [ ] Agent 工具集定义：
  - `search_knowledge_base`: RAG 检索
  - `list_documents`: 列出知识库文档
  - `get_document_info`: 获取文档详情
  - `compare_documents`: 多文档对比
- [ ] Router Agent（意图分类 → 路由到子 Agent）
- [ ] ReAct Agent (Thought → Action → Observation 循环)
- [ ] LangGraph StateGraph 编排：
  ```
  START → router → [rag_agent / sql_agent / summary_agent / react_agent] → END
  ```
- [ ] 对话记忆管理（Redis 存储上下文，超出窗口自动摘要压缩）
- [ ] Agent 执行过程可视化（返回思考链）
- [ ] Agent 超时 + 最大步数限制

**验证标准**：
```bash
# 复杂对比问题
curl -X POST /api/v1/kb/1/qa -d '{
  "question": "对比2023年和2024年的营收目标，分析增长率和主要差异",
  "stream": true
}'
# 返回思考链 + 最终答案（多轮 Agent 协作完成）
```

---

### Phase 6：对话管理与记忆（第 6 周）

**目标**：完整的多轮对话体验

#### 任务清单
- [ ] 对话 CRUD API（创建/列表/详情/删除）
- [ ] 对话标题自动生成（用 LLM 根据首条消息生成）
- [ ] 消息列表 API（分页）
- [ ] 多轮对话上下文管理：
  - 短期记忆：Redis 存储最近 N 轮（默认 10 轮）
  - 长期记忆：对话摘要存入 MySQL，超出窗口自动摘要
- [ ] 对话搜索（按关键词搜索历史对话）

---

### Phase 7：企业特性（第 6 周）

**目标**：权限、审计、多知识库等企业级功能

#### 任务清单
- [ ] 多知识库隔离（用户只能看到自己有权限的知识库）
- [ ] 知识库成员管理（owner / editor / viewer）
- [ ] 操作审计日志（记录所有关键操作：上传/删除/问答/权限变更）
- [ ] 文档删除（软删除 + Chroma 级联删除）
- [ ] 用户管理后台（管理员：用户列表/角色分配/禁用）
- [ ] 系统配置（Embedding 模型切换、分块参数、LLM 参数）
- [ ] 限流（基于 Redis + ip/user_id，可配置）

---

### Phase 8：前端界面（第 7 周）

**目标**：可用的 Web 管理后台 + 问答界面

#### 页面清单
- [ ] 登录/注册页
- [ ] 首页（仪表盘：知识库数量/文档数/问答次数统计）
- [ ] 知识库管理（创建/编辑/删除知识库）
- [ ] 文档管理（上传/列表/预览/删除/处理状态）
- [ ] 问答界面（对话列表 + 聊天窗口 + 流式输出 + 引用展示）
- [ ] 用户管理（管理员）
- [ ] 审计日志（管理员）

---

### Phase 9：测试与优化（第 7-8 周）

**目标**：稳定、高性能、可交付

#### 任务清单
- [ ] 单元测试（pytest + pytest-asyncio，覆盖率 > 80%）
- [ ] API 集成测试（httpx + pytest）
- [ ] RAG 评估：
  - 构建测试问题集 + 标准答案
  - 评估指标：Recall@5 / MRR / Faithfulness
- [ ] 性能优化：
  - 数据库查询优化（索引、N+1 问题）
  - Chroma 检索批量化
  - Redis 缓存预热
- [ ] Docker Compose 生产配置（多阶段构建、健康检查、资源限制）
- [ ] API 文档完善（OpenAPI tags / description / examples）
- [ ] README + 部署文档

---

### Phase 10：扩展加分项（第 8 周+）

以下为可选扩展，每完成一项都是简历亮点：

| 扩展项 | 技术点 | 工作量 |
|--------|--------|--------|
| **知识图谱增强** | Neo4j + GraphRAG，实体关系问答 | 3-5 天 |
| **模型服务化** | BGE-M3 独立部署为 gRPC 服务，GPU 加速 | 2-3 天 |
| **监控可观测** | Prometheus + Grafana 面板（QPS/延迟/Token消耗） | 2-3 天 |
| **CI/CD** | GitHub Actions 自动化测试 + Docker 镜像构建推送 | 1-2 天 |
| **本地模型切换** | Ollama 集成，完全离线运行 | 1 天 |
| **企微/飞书机器人** | Webhook 回调 + 消息卡片 | 3-5 天 |
| **文档对比** | 两个文档 Diff 高亮 + 语义对比 | 2-3 天 |
| **多模态支持** | 图片理解（Qwen-VL）+ 表格问答 | 3-5 天 |
| **细粒度权限** | 文档级/分段级权限控制 | 1-2 天 |
| **向量库到 Milvus** | Chroma → Milvus 迁移适配器 | 2-3 天 |

---

## 八、环境配置指南

### 8.1 开发环境要求

| 工具 | 版本 | 用途 |
|------|------|------|
| Python | 3.12+ | 后端开发 |
| Node.js | 20 LTS | 前端开发 |
| Docker Desktop | 最新 | 服务容器化 |
| Git | 2.40+ | 版本控制 |
| CUDA (可选) | 12.x | GPU 加速 Embedding/LLM |

### 8.2 `.env` 配置文件

```env
# ========== 应用配置 ==========
APP_NAME=EnterpriseQA
APP_VERSION=1.0.0
DEBUG=true
SECRET_KEY=your-secret-key-change-in-production
API_PREFIX=/api/v1

# ========== 数据库 ==========
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_USER=eqa_user
MYSQL_PASSWORD=eqa_password_2024
MYSQL_DATABASE=enterprise_qa
MYSQL_POOL_SIZE=20
MYSQL_POOL_RECYCLE=3600

# ========== Redis ==========
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# ========== MinIO ==========
MINIO_ENDPOINT=localhost:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET=documents
MINIO_SECURE=false

# ========== Chroma ==========
CHROMA_PERSIST_DIR=./data/chroma
CHROMA_COLLECTION_PREFIX=kb_

# ========== BGE-M3 Embedding ==========
EMBEDDING_MODEL=BAAI/bge-m3
EMBEDDING_DEVICE=cuda  # 或 cpu
EMBEDDING_BATCH_SIZE=32
EMBEDDING_NORMALIZE=true

# ========== Reranker ==========
RERANKER_MODEL=BAAI/bge-reranker-v2-m3
RERANKER_DEVICE=cuda
RERANKER_TOP_K=5

# ========== LLM 配置 (OpenAI 兼容) ==========
LLM_BACKEND=ollama  # ollama 或 openai
LLM_BASE_URL=http://localhost:11434/v1  # Ollama
LLM_API_KEY=ollama  # Ollama 不需要真实 key
LLM_MODEL=qwen2.5:14b
LLM_MAX_TOKENS=2048
LLM_TEMPERATURE=0.1

# 如果使用 OpenAI API:
# LLM_BACKEND=openai
# LLM_BASE_URL=https://api.openai.com/v1
# LLM_API_KEY=sk-xxxxx
# LLM_MODEL=gpt-4o

# ========== JWT ==========
JWT_SECRET_KEY=your-jwt-secret-key
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=60
JWT_REFRESH_TOKEN_EXPIRE_DAYS=7

# ========== Celery ==========
CELERY_BROKER_URL=redis://localhost:6379/1
CELERY_RESULT_BACKEND=redis://localhost:6379/2

# ========== 分块配置 ==========
CHUNK_SIZE=512
CHUNK_OVERLAP=64
CHUNK_SEPARATORS=["\n\n","\n","。","！","？","；","，"," "]

# ========== 限流 ==========
RATE_LIMIT_ENABLED=true
RATE_LIMIT_PER_MINUTE=60
RATE_LIMIT_PER_DAY=1000
```

### 8.3 Docker Compose (`docker/docker-compose.yml`)

> **部署模式说明（2026-08 定稿）**：本机 Windows 无法运行 Docker Desktop（WSL2 安装失败），
> 故基础设施（MySQL/Redis/MinIO）通过 Docker 部署在**阿里云 ECS**（公网 IP `118.178.93.114`，
> 4核8G Ubuntu 22.04），后端代码在本机运行并直连云端。开发期文件存储走本地
> （`STORAGE_TYPE=local`），MinIO 可选。`docker compose` 命令均在服务器上执行（SSH）。

```yaml
version: "3.8"

services:
  mysql:
    image: mysql:8.0
    container_name: eqa-mysql
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: root_password
      MYSQL_DATABASE: enterprise_qa
      MYSQL_USER: eqa_user
      MYSQL_PASSWORD: eqa_password_2024
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql
      - ./mysql/init.sql:/docker-entrypoint-initdb.d/init.sql
    command: --character-set-server=utf8mb4 --collation-server=utf8mb4_unicode_ci
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: eqa-redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
      - ./redis/redis.conf:/usr/local/etc/redis/redis.conf
    command: redis-server /usr/local/etc/redis/redis.conf
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  minio:
    image: minio/minio:latest
    container_name: eqa-minio
    restart: unless-stopped
    environment:
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD: minioadmin
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - minio_data:/data
    command: server /data --console-address ":9001"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 10s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: ../backend
      dockerfile: Dockerfile
    container_name: eqa-backend
    restart: unless-stopped
    env_file:
      - ../.env
    ports:
      - "8000:8000"
    volumes:
      - ../backend:/app
      - chroma_data:/app/data/chroma
      - upload_data:/app/data/uploads
    depends_on:
      mysql:
        condition: service_healthy
      redis:
        condition: service_healthy
      minio:
        condition: service_healthy
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload

  celery_worker:
    build:
      context: ../backend
      dockerfile: Dockerfile
    container_name: eqa-celery
    restart: unless-stopped
    env_file:
      - ../.env
    volumes:
      - ../backend:/app
      - chroma_data:/app/data/chroma
      - upload_data:/app/data/uploads
    depends_on:
      - redis
      - mysql
    command: celery -A tasks.celery_app worker -l info -c 4

volumes:
  mysql_data:
  redis_data:
  minio_data:
  chroma_data:
  upload_data:
```

---

## 九、简历写法参考

### 项目描述模板

> **企业文档智能问答平台** | 个人项目 | 2024.07 - 2024.09
>
> 从零设计并实现了一个基于 RAG + Multi-Agent 架构的企业级文档智能问答系统，支持多格式文档解析、混合检索、多 Agent 协作推理和流式对话。
>
> - **后端**：Python + FastAPI 异步框架，SQLAlchemy 2.0 + MySQL + Redis + Chroma 多模态数据层
> - **RAG 引擎**：BGE-M3 混合检索（稠密 1024d + 稀疏 BM25），RRF 融合 + BGE-Reranker-v2 重排序，端到端召回率 92%+
> - **Agent 编排**：基于 LangGraph 实现 Router/ReAct/Summary 多 Agent 状态图，支持复杂多步推理和工具调用
> - **工程化**：Docker Compose 一键部署，Celery 异步任务解耦，Redis 多轮对话记忆，JWT + RBAC 权限控制
> - **可观测性**：Prometheus + Grafana 监控面板，结构化日志，全链路审计追踪

### 面试可能问到的问题（提前准备）

1. **BGE-M3 的稠密和稀疏向量是怎么融合的？RRF 公式是什么？**
2. **Chroma 的底层存储原理？与 Milvus 的区别？什么场景迁移？**
3. **如何处理长文档超出 LLM 上下文窗口？**
4. **Agent 的 ReAct 模式具体怎么实现的？**
5. **多轮对话记忆的摘要压缩策略？**
6. **如何评估 RAG 系统的检索质量？**
7. **Celery 任务失败怎么重试？幂等性怎么保证？**
8. **数据库索引设计思路？为什么选 MySQL 而不是 PostgreSQL？**

---

## 十、开始行动

建议按 Phase 1 → Phase 2 → ... → Phase 9 的顺序推进。每完成一个 Phase，git commit 一次，方便在简历上展示完整的 commit 历史。

当前下一步：**Phase 1 — 初始化项目结构 + Docker 环境**。

需要我现在开始创建项目骨架代码吗？
